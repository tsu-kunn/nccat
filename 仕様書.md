# nccat 仕様書

## 1. 概要

`cat`コマンドの簡易的な代替として、行番号とシンタックスハイライト機能を提供することを目的とした.NETコンソールアプリケーション。
指定されたソースコードファイルを読み込み、ターミナル上で見やすく色付けして表示する。

主な機能は以下の通り。
- ファイルまたは標準入力からのテキスト表示
- 行番号の表示（右寄せ）
- シンタックスハイライト
  - 対応言語: C, C++, C#, Rust, JavaScript, Ruby
  - ブロックコメント (`/* ... */`)、単一行コメント (`//`, `#` 等)、文字列リテラル (`"..."`) の認識

## 2. 開発環境

- **言語**: C#
- **フレームワーク**: .NET 8.0
- **ビルドシステム**: .NET SDK
- **開発スタイル**:
  - `Microsoft.NET.Sdk` スタイルのプロジェクト形式。
  - Nullable参照型 (`<Nullable>enable</Nullable>`) を有効化。

## 3. 実装仕様

### 3.1. ファイル構成

```
/
├── nccat.csproj        # プロジェクト定義ファイル
├── Program.cs          # アプリケーションのメインロジック
├── 仕様書.md           # このドキュメント
├── keyword/            # 各言語のシンタックス定義を格納
│   ├── BaseKeyword.cs  # キーワード定義のベースクラス
│   ├── C.cs            # C言語のキーワード定義
│   ├── Cpp.cs          # C++のキーワード定義
│   ├── Csharp.cs       # C#のキーワード定義
│   ├── Rust.cs         # Rustのキーワード定義
│   ├── JavaScript.cs   # JavaScriptのキーワード定義
│   └── Ruby.cs         # Rubyのキーワード定義
└── MtoLib/             # 汎用ライブラリ
    └── ColorAnsiEscapeCode.cs # コンソール色付け用ANSIエスケープコード生成
```

### 3.2. コーディングルール

ソースコードから見て取れる主なコーディング規約は以下の通り。

- **命名規則**:
  - クラス名、メソッド名: `UpperCamelCase` (例: `HighlightRule`, `GetRulesForFile`)
  - ローカル変数、引数: `lowerCamelCase` (例: `filePath`, `inBlockComment`)
- **コードスタイル**:
  - 型推論に `var` を積極的に使用。
  - 波括弧 `{}` は新しい行から開始する（Allmanスタイル）。
  - LINQ (`OrderByDescending`, `Select`など) を活用したデータ処理。
  - `using`宣言はファイルの先頭にまとめる。
- **その他**:
  - Null許容参照型を有効にしており、`string?` のように Nullになりうる変数を明示。

### 3.3. 実装に基づく仕様

アプリケーションは、状態管理（ステートマシン）と優先度ベースの正規表現エンジンを組み合わせたハイブリッドなアプローチでシンタックスハイライトを実現している。

#### 3.3.1. 起動と入力処理

1.  `Main`メソッドがエントリーポイント。
2.  コマンドライン引数にファイルパスが指定されていれば、そのファイルを対象とする。
3.  リダイレクトによる標準入力があれば、それを対象とする。
4.  ファイルパスも標準入力もなければ、使用方法を表示して終了する。

#### 3.3.2. ハイライトルールの動的選択

1.  `GetRulesForFile` メソッドが、引数で渡されたファイルパスの拡張子を判定する。
    - 対応拡張子: `.c`, `.h`, `.cpp`, `.hpp`, `.cc`, `.cs`, `.rs`, `.js`, `.rb`
2.  拡張子に応じて、`keyword`ディレクトリ内の対応するクラスからJSON形式のルール文字列を読み込む。
3.  JSONをデシリアライズし、ハイライトに使用する正規表現ルールのリスト (`List<HighlightRule>`) を構築する。各ルールは「正規表現パターン」「色」「優先度」を持つ。
4.  単一行コメントのルールは、各言語の定義（例: C言語の`//`, Rubyの`#`）に基づいて動的に生成される。
5.  ファイルパスがない（パイプ入力）場合や、未対応の拡張子の場合は、空のルールリストが返され、ハイライトは行われない。

#### 3.3.3. メインハイライト処理 (`processContent`)

1.  行番号は5桁のスペースを確保し、右寄せで表示される。
2.  `inBlockComment` という `bool` 型のフラグを保持し、複数行にまたがるブロックコメントの状態を管理する。
3.  入力ストリームを1行ずつ読み込み、各行をさらに細かいセグメントに分けて処理する。
4.  **ブロックコメントの処理**:
    - `inBlockComment`が`true`の場合、行内で`*/`を探す。
    - 見つかるまで、または行末までをコメント（緑色）として処理する。
    - `*/`が見つかった時点で`inBlockComment`を`false`に戻す。
5.  **通常コードの処理**:
    - `inBlockComment`が`false`の場合、行内で`/*`（ブロックコメント開始）と`"`（文字列リテラル開始）のどちらが先に出現するかを調べる。
    - **`"`が先の場合**: 対応する閉じ`"`までを文字列リテラル（黄色）として処理する。この区間内の`/*`は無視される。
    - **`/*`が先の場合**: `/*`より前の部分を後述の`HighlightText`メソッドで処理し、`inBlockComment`を`true`に設定する。
    - **どちらもない場合**: 行全体を`HighlightText`メソッドで処理する。

#### 3.3.4. テキストセグメントのハイライト (`HighlightText`)

- ブロックコメントや文字列リテラル以外の「通常のコード」セグメントを引数に取る。
- 選択された言語の全正規表現ルールをテキストに適用し、合致するすべての候補をリストアップする。
- マッチした候補を「開始位置」→「優先度」の順でソートする。
- ソートされたリストを先頭から見ていき、重複するマッチ（より優先度の低いもの）を排除する。
- 最終的に残った、重複しないハイライト候補を基に、ANSIエスケープコードを挿入して色付けされた文字列を構築する。

## 4. ビルド仕様

- **要件**: .NET 8.0 SDK
- **ビルド**: プロジェクトのルートディレクトリで以下のコマンドを実行する。
  ```sh
  dotnet build
  ```
- **実行**:
  - ファイルを指定する場合:
    ```sh
    dotnet run -- <ファイルパス>
    ```
    例: `dotnet run -- Program.cs`
  - パイプで渡す場合:
    ```sh
    cat <ファイルパス> | dotnet run
    ```

### 4.1 リリースビルドについて
1. 実行先に.NETがインストールされている場合

  これは Framework-Dependent（フレームワーク依存） と呼ばれる発行方法です。
  実行先のPCに、アプリケーションがターゲットとする.NETランタイム（このプロジェクトでは.NET8.0）が既にインストールされていることを前提とします。

  特徴:
   - 配布されるファイルのサイズが非常に小さい。
   - 実行には、事前に.NET 8.0ランタイムのインストールが必要。

  コマンド:
  特定のOS向けに実行可能ファイルを作成します。<RID>の部分をターゲットに合わせて変更してください。
   - Linux (64-bit) の場合:
   1   dotnet publish -c Release -r linux-x64
   - Windows (64-bit) の場合:
   1   dotnet publish -c Release -r win-x64
   - macOS (Intel) の場合:

   1   dotnet publish -c Release -r osx-x64
   - macOS (Apple Silicon) の場合:
   1   dotnet publish -c Release -r osx-arm64

  解説:
   - -c Release: デバッグ情報を含まない、最適化されたリリースビルドを作成します。
   - -r <RID>: ランタイムID（Runtime Identifier）を指定し、特定のOS向けの実行ファイルを作成します。

   RID: https://learn.microsoft.com/ja-jp/dotnet/core/rid-catalog

  配布:
  コマンド実行後、bin/Release/net8.0/<RID>/publish/
  ディレクトリが作成されます。この`publish`ディレクトリの中身すべてを配布先にコピーしてください。

  ---

2. 実行先に.NETがインストールされていない場合

  これは Self-Contained（自己完結型） と呼ばれる発行方法です。
  アプリケーションと共に、実行に必要な.NETランタイムのファイルも一緒にパッケージングします。

  特徴:
   - .NETがインストールされていないPCでもそのまま実行できる。
   - ランタイムが含まれるため、配布されるファイルのサイズが大きくなる（数十MB〜）。

  コマンド:
  -r <RID>の指定が必須です。

   - Linux (64-bit) の場合:
   1   dotnet publish -c Release -r linux-x64 --self-contained true
   - Windows (64-bit) の場合:

   1   dotnet publish -c Release -r win-x64 --self-contained true

  解説:
   - --self-contained true: .NETランタイムをアプリケーションに同梱するように指定します。

  配布:
  上記と同様に、bin/Release/net8.0/<RID>/publish/
  ディレクトリの中身すべてを配布先にコピーすれば、.NETのインストールなしで実行できます。

  ---

  （任意）さらに配布しやすくするために

  dotnet publishにオプションを追加することで、単一の実行可能ファイルとして出力することも可能です。

  例（Linux向け自己完結・単一ファイル）:

   1 dotnet publish -c Release -r linux-x64 --self-contained true -p:PublishSingleFile=true
  この -p:PublishSingleFile=true
  オプションを追加すると、publishディレクトリに依存ファイルが散らばらず、nccatという単一の実行ファイル
  （といくつかの関連ファイル）にまとめられるため、配布がより簡単になります。

